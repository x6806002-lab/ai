# === 核心构建工具 - 必须最先安装以兼容 Python 3.13 ===
# 升级 pip 自身，以更好地处理依赖解析
pip>=24.0
# 使用新版 setuptools，它不再依赖 distutils
setuptools>=70.0.0
# 鼓励使用预编译的 wheel 包，避免源码编译
wheel

# === 应用依赖 - 指定较新版本以获取预编译包 ===
streamlit>=1.28.0
numpy>=1.26.0
pandas>=2.1.0
matplotlib>=3.8.0
seaborn>=0.13.0
